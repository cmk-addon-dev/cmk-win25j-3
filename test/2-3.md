### @hideIteration true
### @flyoutOnly false
### @explicitHints true
### @hideDone true

# 2-3

## ちゃれんじもんだい に ちょうせんしよう！ 

```template
player.onChat("go", function () {})
```

```ghost
player.onChat("go", function () {
    mobs.spawn(PRIMED_TNT, posCamera(0, 0, 0))
    blocks.place(GRASS, posCamera(0, 0, 0))
})
```

